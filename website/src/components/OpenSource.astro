<section class="os">
	<div class="wrap">
		<p class="kicker">Open source · AGPL-3.0</p>
		<h2 class="sec-head">Audit it. Extend it.<br />Run it yourself.</h2>
		<p class="lede">
			Indelible is open-source under the AGPL-3.0. The full source opens on GitHub at launch,
			because the archive you trust with your reading should be one you can read too.
		</p>
		<a class="repo" href="https://github.com/useindelible/indelible">github.com/useindelible/indelible</a>
	</div>
</section>

<style>
	.os {
		padding: 96px 0;
		text-align: center;
	}

	.os .kicker {
		display: block;
		margin-bottom: 20px;
	}

	.lede {
		margin: 16px auto 0;
		max-width: 46ch;
		font-size: 16px;
		line-height: 1.6;
		color: var(--ink-soft);
	}

	.repo {
		display: inline-block;
		margin-top: 28px;
		font-family: var(--font-mono);
		font-size: 13px;
		color: var(--accent);
		border-bottom: 1px solid var(--accent);
		padding-bottom: 2px;
	}

	.repo:hover {
		color: var(--ink);
		border-color: var(--ink);
	}
</style>
