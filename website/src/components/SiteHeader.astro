<header class="site-header">
	<div class="wrap bar">
		<a class="brand" href="/">
			<svg viewBox="0 0 200 200" aria-hidden="true">
				<rect x="20" y="20" width="160" height="160" rx="42" fill="currentColor"></rect>
				<g class="counter">
					<rect x="94" y="48" width="12" height="40" rx="6"></rect>
					<rect x="94" y="112" width="12" height="40" rx="6"></rect>
					<rect x="48" y="94" width="40" height="12" rx="6"></rect>
					<rect x="112" y="94" width="40" height="12" rx="6"></rect>
					<circle cx="100" cy="100" r="19" class="hub"></circle>
				</g>
				<circle cx="100" cy="100" r="7" fill="currentColor"></circle>
			</svg>
			<span>Indelible</span>
		</a>
		<nav>
			<a href="/docs/">Docs</a>
			<a href="https://github.com/useindelible/indelible">GitHub</a>
			<a class="btn" href="/docs/self-hosting/install/">Get started</a>
		</nav>
	</div>
</header>

<style>
	.site-header {
		border-bottom: 1px solid var(--hairline);
	}

	.bar {
		display: flex;
		align-items: center;
		justify-content: space-between;
		height: 64px;
	}

	.brand {
		display: flex;
		align-items: center;
		gap: 10px;
		font-family: var(--font-serif);
		font-size: 20px;
		font-weight: 600;
		letter-spacing: 0.01em;
		color: var(--ink);
	}

	.brand svg {
		width: 26px;
		height: 26px;
		color: var(--accent);
	}

	.counter rect,
	.counter .hub {
		fill: var(--paper);
	}

	nav {
		display: flex;
		align-items: center;
		gap: 28px;
		font-size: 14px;
		color: var(--ink-soft);
	}

	nav a:not(.btn):hover {
		color: var(--ink);
	}

	@media (max-width: 480px) {
		.brand span {
			display: none;
		}

		nav {
			gap: 18px;
		}
	}
</style>
